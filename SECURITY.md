# セキュリティ・権限チェックリスト — このキットができること／できないこと

※このページの制限は**Claude Code使用時のみ**有効です。Codex / Gemini CLI をお使いの場合は、各ツール側の権限設定に従います。

「AIに何を許可しているのか分からない」が、いちばんの不安だと思います。
このキットは、**できることを最小限に絞って**作られています。

## ✅ できること（これだけ）

| できること | 目的 |
|-----------|------|
| このキットのフォルダの中のファイルを読む・書く | あなたの設計図（一枚）を刻むため |
| あなたが選んだNotionページの配下に、ページを作る・書く | 設計図をNotionに建てるため（接続した場合のみ） |

## ❌ できないこと（設定でブロックしています）

- ターミナルコマンドの実行（Bash）
- インターネットの閲覧・検索・外部への送信
- APIキー・パスワード・カード情報を尋ねること（もし尋ねてきたら、それは誤作動です。答えずに閉じてください）

## ⚠️ 範囲の外に出ようとすると、その都度あなたの確認が入ること

- このフォルダの**外**のファイルを書き換えること（許可されているのはフォルダの中だけなので、外に触れようとすると確認ダイアログが出ます）
- あなたが選んだページ**以外**のNotionを触ること（Notion接続時に許可したページの範囲でしか動きません）

## この設定はどこにある？

`.claude/settings.json` に書いてあります（Claude Codeが自動で読み込み、上の制限を適用します）。
中身を確認したい方は、開いてそのまま読めます——隠していることは何もありません。

## Notion接続について

- 接続は**任意**です。接続しなくても、設計図はこのフォルダの中で完成します
- 接続時にNotion側で「どのページへのアクセスを許可するか」を選べます。**このキット用のページだけ**を許可すれば十分です
